extends layout

block content
  form(method='POST' action='')
    div.form-group
      label(for='login') Identifiant:
      input#login.form-control(type='text' name='login' required='true')

    div.form-group
      label(for='password') Mot de passe:
      input#password.form-control(type='password' name='password' required='true')

    button.btn.btn-primary(type='submit') Se connecter
  form.form-inline(method='POST' action='/register')
    p Pas de compte ?
    button.btn.btn-secondary(type='submit') S'inscrire

